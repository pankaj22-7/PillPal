import { StyleSheet } from 'react-native';

const elderlyStyles = StyleSheet.create({
  input: {
    fontSize: 18,          // larger text field labels
  },
  button: {
    paddingVertical: 10,   // bigger touch target
  },
  text: {
    fontSize: 18,
  },
});

export default elderlyStyles;
